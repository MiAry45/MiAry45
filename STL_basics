// to store elements in a map
 int n =5;
    map<int,int>mp;
    for(int i =0;i<n;i++){
        mp[i]= i+1;
    }
    for(auto it: mp){
        cout<<it.second<<" ";
// here mp[i] is storing the elements for the key and (i+1) is  storing the element for the values of key
    }
