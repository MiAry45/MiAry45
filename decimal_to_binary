using namespace std;
typedef long long ll;
string dec_to_binary(ll n ){
    string s ="";
    while(n > 0){
        ll v = n%2;
        string a = to_string(v);
        s += a;
        n = n/2;
    }
    reverse(s.begin(),s.end());
    return s;
}
